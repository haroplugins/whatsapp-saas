export default function HomePage() {
  return (
    <main>
      <section className="shell">
        <span className="eyebrow">apps/web</span>
        <h1>Web workspace ready</h1>
        <p>
          Next.js App Router, TypeScript, ESLint and Prettier are configured for
          the main web application.
        </p>
      </section>
    </main>
  );
}
